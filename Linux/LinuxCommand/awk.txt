awk 명령어
-> 이름에 그 기능을 의미하는 
    단어나 약어가 포함되어 있지 않습니다.
-> awk( Aho + Weinberger + Kernighan )

: 1> 파일로 부터 레코드(record)선택,
: 2> record로 데이터 값을 조작하거나 
      데이터화하는 것을 목적으로 사용하는 프로그래밍
// 데이터를 분류한다.
   분류된 텍스트 데이터를 바탕으로 한다.
   패턴 매칭 여부를 검사하거나,
   데이터 조작 및 연산 등의 액션을 수행하고
   그 결과를 출력하는 기능을 수행한다.

패턴, 분류, 텍스트, 조작, 연산, 액션을 하는 프로그램
어떤 기능을 수행할까?? , 어떤 목적을 위해 사용할까??

1. 텍스트 파일의 전체 내용 출력
2. 파일의 특정 필드만 출력.
3. 특정 필등 문자열을 추가햏서 출력.
4. 패턴이 포함된 레코드 출력.
5. 특정 필드에 연산 수행 결과 출력.
6. 필드 값 비교에 따라 레코드 출력